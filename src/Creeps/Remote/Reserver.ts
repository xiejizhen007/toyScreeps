import { Role } from "Creeps/Role";

/**
 * 预定房间
 * 主要由是处理外矿
 */
export class Reserver extends Role {
    controller: StructureController;

    init(): void {
        
    }

    work(): void {

    }
}